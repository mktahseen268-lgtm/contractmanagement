apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-api
  namespace: contract-management
  labels: { app: cm-api, component: api }
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: "25%", maxUnavailable: 0 }
  selector: { matchLabels: { app: cm-api } }
  template:
    metadata:
      labels: { app: cm-api, component: api }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: { matchLabels: { app: cm-api } }
      containers:
        - name: api
          image: ghcr.io/your-org/cm-api:0.3.0   # replace; admission policy verifies cosign sig
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8000, name: http }]
          envFrom:
            - configMapRef: { name: cm-config }
            - secretRef: { name: cm-secrets }
          resources:
            requests: { cpu: 500m, memory: 512Mi }
            limits:   { cpu: "2",   memory: 2Gi }
          readinessProbe:
            httpGet: { path: /healthz/ready, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
      volumes:
        - { name: tmp, emptyDir: {} }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-web
  namespace: contract-management
  labels: { app: cm-web, component: web }
spec:
  replicas: 2
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: "25%", maxUnavailable: 0 } }
  selector: { matchLabels: { app: cm-web } }
  template:
    metadata: { labels: { app: cm-web, component: web } }
    spec:
      automountServiceAccountToken: false
      securityContext: { runAsNonRoot: true, runAsUser: 10001, fsGroup: 10001, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: web
          image: ghcr.io/your-org/cm-web:0.3.0
          ports: [{ containerPort: 3000, name: http }]
          env:
            - { name: NEXT_PUBLIC_API_URL, value: "https://api.example.com" }
          resources: { requests: { cpu: 200m, memory: 256Mi }, limits: { cpu: "1", memory: 1Gi } }
          readinessProbe: { httpGet: { path: /, port: http }, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /, port: http }, periodSeconds: 30 }
          securityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: ["ALL"] } }
          volumeMounts: [{ name: tmp, mountPath: /tmp }, { name: cache, mountPath: /app/.next/cache }]
      volumes: [{ name: tmp, emptyDir: {} }, { name: cache, emptyDir: {} }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-worker
  namespace: contract-management
  labels: { app: cm-worker, component: worker }
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  selector: { matchLabels: { app: cm-worker } }
  template:
    metadata:
      labels: { app: cm-worker, component: worker }
      annotations:
        # The worker exits cleanly on SIGTERM (Celery default) — give it 60s to drain in-flight
        # OCR + signing jobs before the kubelet sends SIGKILL.
        prometheus.io/scrape: "true"
        prometheus.io/port: "9540"
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 60
      securityContext: { runAsNonRoot: true, runAsUser: 10001, fsGroup: 10001, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: worker
          image: ghcr.io/your-org/cm-api:0.3.0
          command: ["celery"]
          args: ["-A", "app.celery_app", "worker", "--loglevel=info", "--concurrency=2", "-Q", "default,ocr"]
          envFrom: [{ configMapRef: { name: cm-config } }, { secretRef: { name: cm-secrets } }]
          resources: { requests: { cpu: 500m, memory: 512Mi }, limits: { cpu: "2", memory: 2Gi } }
          # Liveness: `celery inspect ping` round-trips through the broker — if the worker is
          # alive AND can talk to Redis, this succeeds. failureThreshold=3 means the worker
          # gets ~3 minutes of broker outage before restart (avoids restart storms during
          # Redis blips). startupProbe gives it 2 minutes to boot before the others run.
          startupProbe:
            exec:
              command: ["sh", "-c", "celery -A app.celery_app inspect ping -d celery@$HOSTNAME --timeout 10"]
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            exec:
              command: ["sh", "-c", "celery -A app.celery_app inspect ping -d celery@$HOSTNAME --timeout 10"]
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 3
          # Readiness: the worker is "ready" once it has registered with the broker. We use
          # the same ping but a tighter cadence + lower threshold so a wedged worker stops
          # taking new tasks within ~30s.
          readinessProbe:
            exec:
              command: ["sh", "-c", "celery -A app.celery_app inspect ping -d celery@$HOSTNAME --timeout 5"]
            periodSeconds: 15
            timeoutSeconds: 8
            failureThreshold: 2
          securityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: ["ALL"] } }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
      volumes:
        - { name: tmp, emptyDir: {} }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cm-beat
  namespace: contract-management
  labels: { app: cm-beat, component: beat }
spec:
  replicas: 1     # SINGLETON — Celery beat must not run multi-instance
  strategy: { type: Recreate }
  selector: { matchLabels: { app: cm-beat } }
  template:
    metadata: { labels: { app: cm-beat, component: beat } }
    spec:
      securityContext: { runAsNonRoot: true, runAsUser: 10001, fsGroup: 10001, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: beat
          image: ghcr.io/your-org/cm-api:0.3.0
          command: ["celery"]
          args: ["-A", "app.celery_app", "beat", "--loglevel=info"]
          envFrom: [{ configMapRef: { name: cm-config } }, { secretRef: { name: cm-secrets } }]
          resources: { requests: { cpu: 100m, memory: 128Mi }, limits: { cpu: 500m, memory: 256Mi } }
          securityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: ["ALL"] } }

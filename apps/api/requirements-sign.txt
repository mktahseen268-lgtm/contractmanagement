# Optional cryptographic-signing extras (RFI T-4). Install only for PAdES sealing:
#   pip install -r requirements-sign.txt
# Then set SIGNING_PROVIDER=pades and SIGNING_CERT_PATH=/path/to/cert.p12 (+ password).
# The default SIGNING_PROVIDER=internal needs none of these.
-r requirements.txt
pyHanko==0.25.1
